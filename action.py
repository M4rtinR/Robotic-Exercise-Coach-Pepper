class Action:

    def __init__(self, behaviour, data):
        self.behaviour = behaviour
        self.data = data

    def to_string(self):
        # Use behaviour and data to generate utterance, based on random choice from utterance list for each behaviour.
        utterance = ""

        return utterance
